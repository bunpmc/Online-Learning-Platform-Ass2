@using OnlineLearningPlatformAss2.Service.Services.Interfaces
@using System.Security.Claims
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>@ViewData["Title"] - LearnHub</title>
  <meta name="description" content="LearnHub - Premium online learning platform with thousands of courses from expert instructors.">
  
  <!-- Preload critical resources -->
  <link rel="preload" href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:wght@300;400;500;600;700;900&display=swap" as="style">
  
  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:wght@300;400;500;600;700;900&display=swap" rel="stylesheet">
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  
  <!-- Custom CSS -->
  <link rel="stylesheet" href="~/css/modern-design-system.css" asp-append-version="true"/>
  <link rel="stylesheet" href="~/css/layout.css" asp-append-version="true"/>
  <link rel="stylesheet" href="~/css/components.css" asp-append-version="true"/>
  <link rel="stylesheet" href="~/css/animations.css" asp-append-version="true"/>
  <style>
    .search-spinner { position: absolute; right: 1rem; top: 50%; transform: translateY(-50%); pointer-events: none; }
    .search-preview-dropdown { position: absolute; top: 100%; left: 0; right: 0; background: white; border-radius: 12px; box-shadow: 0 10px 25px rgba(0,0,0,0.1); margin-top: 0.5rem; z-index: 1000; overflow: hidden; }
    .search-preview-item { display: flex; align-items: center; padding: 0.75rem 1rem; text-decoration: none; color: inherit; transition: background 0.2s; border-bottom: 1px solid #f8f9fa; }
    .search-preview-item:hover { background: #f0f7ff; }
    .search-preview-item img { width: 60px; height: 34px; object-fit: cover; border-radius: 4px; margin-right: 1rem; }
    .search-preview-item .item-title { font-weight: 600; font-size: 0.9rem; display: -webkit-box; -webkit-line-clamp: 1; -webkit-box-orient: vertical; overflow: hidden; }
    .search-preview-item .item-meta { font-size: 0.75rem; color: #6c757d; }
  </style>
  
  <!-- Page-specific styles -->
  @await RenderSectionAsync("Styles", required: false)
  
  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>??</text></svg>">
</head>

<body>
@await Component.InvokeAsync("UserStatus")

<!-- Navigation -->
<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-white">
    <div class="container">
      <!-- Logo -->
      <a class="navbar-brand" asp-page="/Index">
        LearnHub
      </a>
      
      <!-- Mobile Toggle -->
      <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarNav">
        <!-- Main Navigation -->
        <ul class="navbar-nav me-auto">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
              Explore
            </a>
            <ul class="dropdown-menu">
              <li>
                <a class="dropdown-item" asp-page="/Course/Browse">
                  Browse All Courses
                </a>
              </li>
              <li>
                <a class="dropdown-item" asp-page="/LearningPath/Browse">
                  Learning Paths
                </a>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <a class="dropdown-item" asp-page="/Assessment/Start">
                  Skill Assessment
                </a>
              </li>
            </ul>
          </li>
        </ul>
        
        <!-- Search Bar -->
        <div class="search-wrapper position-relative me-3">
          <form class="d-none d-lg-flex" method="get" asp-page="/Course/Browse" id="globalSearchForm">
            <div class="search-container">
              <i class="bi bi-search search-icon"></i>
              <input type="text" 
                     class="search-input" 
                     name="searchTerm" 
                     id="globalSearchInput"
                     placeholder="What do you want to learn?"
                     autocomplete="off">
              <div id="searchLoadingSpinner" class="search-spinner d-none">
                <div class="spinner-small"></div>
              </div>
            </div>
          </form>
          <div id="searchPreviewResults" class="search-preview-dropdown d-none">
            <div class="preview-header">
              <span class="small text-muted fw-bold">COURSE SUGGESTIONS</span>
            </div>
            <div id="previewItemsList"></div>
            <div class="preview-footer">
              <a href="javascript:void(0)" onclick="document.getElementById('globalSearchForm').submit()" class="view-all-results">
                View all results <i class="bi bi-arrow-right"></i>
              </a>
            </div>
          </div>
        </div>
        
        <!-- User Navigation -->
        <ul class="navbar-nav">
          @if (User.Identity?.IsAuthenticated == true)
          {
            <li class="nav-item">
              <a class="nav-link" asp-page="/Course/MyCourses">
                <i class="bi bi-journal-bookmark me-1"></i>My Learning
              </a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                <i class="bi bi-person-circle me-1"></i>@User.Identity.Name
              </a>
              <ul class="dropdown-menu dropdown-menu-end">
                @if (User.IsInRole("Admin"))
                {
                  <li><a class="dropdown-item fw-bold text-danger" asp-page="/Admin/Dashboard">Admin Control Center</a></li>
                  <li><hr class="dropdown-divider"></li>
                }
                @if (User.IsInRole("Instructor"))
                {
                  <li><a class="dropdown-item fw-bold text-primary" asp-page="/Instructor/Dashboard">Instructor Dashboard</a></li>
                  <li><hr class="dropdown-divider"></li>
                }
                else
                {
                  <li><a class="dropdown-item" asp-page="/Instructor/BecomeInstructor">Become an Instructor</a></li>
                }
                <li><a class="dropdown-item" asp-page="/LearningPath/MyPaths">My Paths</a></li>
                <li><a class="dropdown-item" asp-page="/Order/MyOrders">My Orders</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" asp-page="/User/Profile">My Profile</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item text-danger" asp-page="/User/Logout">Log Out</a></li>
              </ul>
            </li>
          }
          else
          {
            <li class="nav-item">
              <a class="nav-link" asp-page="/User/Login">Log In</a>
            </li>
            <li class="nav-item">
              <a class="btn btn-primary" asp-page="/User/Register">Join for Free</a>
            </li>
          }
        </ul>
      </div>
    </div>
  </nav>
</header>

<!-- Main Content -->
<main>
  @if (TempData["SuccessMessage"] != null)
  {
    <div class="alert-container">
      <div class="container">
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <i class="bi bi-check-circle-fill me-2"></i>
          @TempData["SuccessMessage"]
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      </div>
    </div>
  }
  
  @RenderBody()
</main>

<!-- Footer -->
<footer class="footer">
  <div class="container">
    <div class="row">
      <div class="col-lg-4">
        <div class="footer-brand">
          <span>LearnHub</span>
        </div>
        <p class="footer-description">
          A high-quality online learning platform that helps you develop skills 
          and advance your career with expert-led courses.
        </p>
        <div class="social-links">
          <a href="#"><i class="bi bi-facebook"></i></a>
          <a href="#"><i class="bi bi-twitter"></i></a>
          <a href="#"><i class="bi bi-linkedin"></i></a>
          <a href="#"><i class="bi bi-youtube"></i></a>
        </div>
      </div>
      <div class="col-lg-8">
        <div class="row">
          <div class="col-md-3">
            <h6>Company</h6>
            <ul class="footer-links">
              <li><a asp-page="/Index">About Us</a></li>
              <li><a asp-page="/Course/Browse">Browse Courses</a></li>
              <li><a asp-page="/LearningPath/Browse">Learning Paths</a></li>
              <li><a asp-page="/Privacy">Terms & Privacy</a></li>
            </ul>
          </div>
          <div class="col-md-3">
            <h6>Community</h6>
            <ul class="footer-links">
              <li><a asp-page="/User/Profile">My Profile</a></li>
              <li><a asp-page="/Course/MyCourses">My Learning</a></li>
              <li><a asp-page="/Order/MyOrders">My Orders</a></li>
              <li><a asp-page="/Assessment/Start">Assessment</a></li>
            </ul>
          </div>
          <div class="col-md-3">
            <h6>More</h6>
            <ul class="footer-links">
              <li><a asp-page="/Privacy">Security</a></li>
              <li><a asp-page="/Privacy">Status</a></li>
              <li><a asp-page="/Index">Platform Info</a></li>
              <li><a asp-page="/Index">Accessibility</a></li>
            </ul>
          </div>
          <div class="col-md-3">
            <h6>Mobile App</h6>
            <ul class="footer-links">
              <li><a href="https://apple.com/app-store" target="_blank">Download iOS App</a></li>
              <li><a href="https://play.google.com/store" target="_blank">Download Android App</a></li>
            </ul>
          </div>
        </div>
      </div>
    </div>
    <hr>
    <div class="footer-bottom">
      <p>&copy; 2025 LearnHub Inc. All rights reserved.</p>
    </div>
  </div>
</footer>

<!-- Scripts -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>

<!-- Global Search Handler -->
<script>
function handleSearchSubmit(event) {
    const searchInput = document.getElementById('globalSearchInput');
    const searchTerm = searchInput?.value.trim();
    
    console.log('Global search submitted:', searchTerm);
    
    if (!searchTerm) {
        event.preventDefault();
        // If empty, just go to browse page without search
        window.location.href = '/Course/Browse';
        return false;
    }
    
    // Let the form submit normally with searchTerm parameter
    return true;
}

// Auto-submit on Enter key for better UX
document.addEventListener('DOMContentLoaded', function() {
    const globalSearchInput = document.getElementById('globalSearchInput');
    const searchPreviewResults = document.getElementById('searchPreviewResults');
    const previewItemsList = document.getElementById('previewItemsList');
    
    let debounceTimer;

    if (globalSearchInput) {
        globalSearchInput.addEventListener('input', function() {
            const searchTerm = this.value.trim();
            const spinner = document.getElementById('searchLoadingSpinner');
            
            clearTimeout(debounceTimer);
            if (searchTerm.length < 2) {
                searchPreviewResults.classList.add('d-none');
                spinner?.classList.add('d-none');
                return;
            }

            spinner?.classList.remove('d-none');
            debounceTimer = setTimeout(async () => {
                try {
                    const response = await fetch(`/Course/Browse?handler=SearchPreview&searchTerm=${encodeURIComponent(searchTerm)}`);
                    if (response.ok) {
                        const data = await response.json();
                        if (data.courses && data.courses.length > 0) {
                            renderSearchPreview(data.courses);
                            searchPreviewResults.classList.remove('d-none');
                        } else {
                            renderEmptyPreview();
                            searchPreviewResults.classList.remove('d-none');
                        }
                    }
                } catch (error) {
                    console.error('Search preview error:', error);
                } finally {
                    spinner?.classList.add('d-none');
                }
            }, 300);
        });

    // Intersection Observer for section animations
    const observerOptions = {
        threshold: 0.1
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');
            }
        });
    }, observerOptions);
        function renderEmptyPreview() {
            searchPreviewResults.innerHTML = `
                <div class="p-3 text-center text-muted">
                    <i class="bi bi-search mb-2 d-block fs-4"></i>
                    <p class="mb-0 small">No matching courses found</p>
                </div>
            `;
        }

        function renderSearchPreview(courses) {
            searchPreviewResults.innerHTML = courses.map(course => `
                <a href="/Course/Details/${course.id}" class="search-preview-item">
                    <img src="${course.imageUrl || 'https://via.placeholder.com/60x34'}" alt="${course.title}">
                    <div class="item-info">
                        <div class="item-title">${course.title}</div>
                        <div class="item-meta">${course.categoryName} • ${course.instructorName}</div>
                    </div>
                </a>
            `).join('');
        }

    document.querySelectorAll('section').forEach(section => {
        observer.observe(section);
    });

        globalSearchInput.addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                e.preventDefault();
                const form = this.closest('form');
                if (form) {
                    const term = globalSearchInput.value.trim();
                    if (term) {
                        globalSearchInput.value = term;
                        form.submit();
                    } else {
                        window.location.href = '/Course/Browse';
                    }
                }
            }
        });
        
        // Hide preview on click outside
        document.addEventListener('click', function(e) {
            if (!globalSearchInput.contains(e.target) && !searchPreviewResults.contains(e.target)) {
                searchPreviewResults.classList.add('d-none');
            }
        });
    }

    function renderSearchPreview(courses) {
        previewItemsList.innerHTML = courses.map(course => `
            <a href="/Course/Details?id=${course.id}" class="preview-item">
                <img src="${course.image || 'https://via.placeholder.com/60x40?text=Course'}" class="preview-img" alt="${course.title}">
                <div class="preview-info">
                    <h5 class="preview-title">${course.title}</h5>
                    <div class="preview-meta">
                        ${course.instructor} • ${course.price}
                    </div>
                </div>
            </a>
        `).join('');
    }
    
    function renderEmptyPreview() {
        previewItemsList.innerHTML = `
            <div class="px-3 py-4 text-center">
                <i class="bi bi-search text-muted d-block mb-2 h4"></i>
                <p class="text-muted small mb-0">No matching courses found</p>
            </div>
        `;
    }

    // Smooth scrolling for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            const targetId = this.getAttribute('href');
            if (targetId === '#' || targetId === '') return;
            
            try {
                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    e.preventDefault();
                    targetElement.scrollIntoView({
                        behavior: 'smooth'
                    });
                }
            } catch (err) {
                // Ignore invalid selectors
            }
        });
    });
});
</script>

@await RenderSectionAsync("Scripts", false)

@* Include Chat Widget for live support (only for authenticated non-admin users) *@
@if (User.Identity?.IsAuthenticated == true && !User.IsInRole("Admin"))
{
    <partial name="_Chatbot" />
}

</body>
</html>
